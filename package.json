{
  "name": "@cranbee/template",
  "version": "0.2.1",
  "description": "Cranbee Template Engine",
  "main": "index.js",
  "scripts": {
    "test": "node test/test.js",
    "patch-release": "npm version patch && npm publish --access=public && git push --follow-tags",
    "minor-release": "npm version minor && npm publish --access=public && git push --follow-tags",
    "generate-samples": "node test/generator.js"
  },
  "author": "DMA <dma@cranbee.com>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/cranbee/template.git"
  },
  "devDependencies": {
    "tape": "^4.6.3"
  }
}
